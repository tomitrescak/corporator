# ========================================================= #
#  QUERIES
# ========================================================= # */

type Query {
  resourceQuery(id: ID!): Resource
  processResourceQuery(id: ID!): Resource
}
