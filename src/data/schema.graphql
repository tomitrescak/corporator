#import User from './generated/prisma.graphql'
#import Query.*, Mutation.* from './schema/notifications.graphql'

type Query {
  authenticate(username: String, password: String): String!
  user(id: ID!): User
}

type Mutation {
  signup(name: String!): User!
}
