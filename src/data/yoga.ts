import * as Yoga from './generated/api';

// this is just a helper file that keeps all the types
// it is used for type safety in resolvers
// please add all new business objects here if you want to use them in resolvers

export { Yoga };
