query BpmnProcessInstancesQuery($input: BpmnProcessInstancesInput!) {
  bpmnProcessInstances(input: $input) {
    id
    status
    process {
      name
      description
      type
    }
    owner {
      name
    }
    tasks {
      dateStarted
      dateFinished
      performer {
        name
      }
      performerRoles
    }
    dateStarted
    dateFinished
  }
}
