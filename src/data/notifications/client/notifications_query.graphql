query NotificationsQuery($input: NotificationsInput) {
  notifications(input: $input) {
    id
    createdAt
    text
    type
    processInstance {
      id
      process {
        name
      }
    }
  }
}
