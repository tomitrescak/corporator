export enum ProcessStatus {
  Draft = 'Draft',
  Proposal = 'Proposal',
  Published = 'Published'
}

export enum InstanceStatus {
  Running = 'running',
  Paused = 'paused',
  Finished = 'finished',
  Aborted = 'aborted'
}
